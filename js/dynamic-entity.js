class DynamicEntity extends Entity2 {
    constructor(x, y, width, height, color) {
        super(x, y, width, height, color);
    }

    update(mapDimension) {
        super.update(mapDimension);
    }

    render(ctx) {
        super.render(ctx);
    }
}